{
  "name": "mfp-mcp-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Developer tooling for mfp-mcp — one-time key management CLI.",
  "scripts": {
    "store-key": "tsx scripts/store-key.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
